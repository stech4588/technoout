import AdminLayout from '@/layouts/admin-layout';import {Head,Link} from '@inertiajs/react';
export default function Dashboard({stats,inquiries,invoices}:any){return <AdminLayout title="Command center"><Head title="Admin dashboard" /><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-4">{stats.map((s:any)=><Link href={s.href} key={s.label} className="rounded-2xl border border-white/10 bg-white/[.035] p-6"><p className="text-sm text-slate-500">{s.label}</p><p className="mt-3 text-4xl font-black text-cyan-300">{s.value}</p></Link>)}</div><div className="mt-8 grid gap-6 xl:grid-cols-2"><Panel title="Recent requests" rows={inquiries} fields={['reference','name','status']} /><Panel title="Recent invoices" rows={invoices} fields={['number','customer_name','status']} /></div></AdminLayout>}
function Panel({title,rows,fields}:any){return <section className="rounded-2xl border border-white/10 bg-white/[.03] p-6"><h2 className="font-bold">{title}</h2><div className="mt-5 space-y-2">{rows.map((row:any)=><div key={row.id} className="grid grid-cols-3 gap-3 rounded-lg bg-white/[.03] p-3 text-sm">{fields.map((f:string)=><span key={f} className="truncate">{String(row[f]??'—')}</span>)}</div>)}</div></section>}
